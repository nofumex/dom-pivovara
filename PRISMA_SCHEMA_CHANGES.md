# Работа с изменениями Prisma Schema после миграции на Neon

## ❓ Нужно ли переносить данные заново?

**НЕТ!** После изменения `schema.prisma` **НЕ нужно** переносить данные заново. 

Данные уже в Neon и останутся там. Нужно только **применить изменения схемы** к существующей базе данных.

---

## 🔄 Как применить изменения схемы

### Вариант 1: `prisma db push` (Быстро, для разработки)

Используется когда нужно быстро применить изменения без создания файлов миграций.

```powershell
# 1. Убедитесь, что .env настроен на Neon
# DATABASE_URL и DIRECT_URL должны указывать на Neon

# 2. Примените изменения схемы
npx prisma db push

# 3. Сгенерируйте Prisma Client
npm run db:generate
```

**Когда использовать:**
- ✅ Быстрое прототипирование
- ✅ Разработка и тестирование
- ✅ Когда не нужна история миграций

**⚠️ Не используйте в production!**

---

### Вариант 2: `prisma migrate dev` (Рекомендуется)

Создает файлы миграций и применяет их к базе данных. Это правильный подход для продакшена.

```powershell
# 1. Убедитесь, что .env настроен на Neon
# DATABASE_URL и DIRECT_URL должны указывать на Neon

# 2. Создайте и примените миграцию
npx prisma migrate dev --name описание_изменений

# Пример:
npx prisma migrate dev --name add_new_field_to_product

# Prisma Client будет автоматически сгенерирован
```

**Когда использовать:**
- ✅ Production окружение
- ✅ Когда нужна история изменений
- ✅ Работа в команде
- ✅ Откат изменений

**Что делает:**
- Создает файл миграции в `prisma/migrations/`
- Применяет изменения к базе данных
- Автоматически генерирует Prisma Client

---

### Вариант 3: `prisma migrate deploy` (Только для production)

Используется только для применения существующих миграций в production.

```powershell
npx prisma migrate deploy
```

**Когда использовать:**
- ✅ При деплое в production
- ✅ Когда миграции уже созданы и проверены

---

## 📝 Типичный workflow после изменения schema.prisma

### В разработке:

```powershell
# 1. Изменили schema.prisma

# 2. Применили изменения
npx prisma migrate dev --name описание_изменений

# 3. Проверили через Prisma Studio
npm run db:studio
```

### В production:

```powershell
# 1. Создали миграцию локально
npx prisma migrate dev --name описание_изменений

# 2. Закоммитили изменения (schema.prisma + папка migrations)

# 3. На сервере применили миграции
npx prisma migrate deploy
```

---

## ⚠️ Важные моменты

### 1. Данные сохраняются

При применении изменений через `db push` или `migrate`:
- ✅ **Существующие данные сохраняются**
- ✅ Добавляются только новые таблицы/поля
- ✅ Удаление полей/таблиц происходит только если явно указано

### 2. Бэкап перед важными изменениями

Перед критичными изменениями (удаление таблиц, изменение типов):
```powershell
# Создайте бэкап через Neon Dashboard или:
pg_dump -d "ВАШ_NEON_CONNECTION_STRING" -F c -f backup_before_migration.dump
```

### 3. Проверка перед применением

```powershell
# Посмотрите, что будет изменено (без применения)
npx prisma migrate dev --create-only --name имя_миграции

# Проверьте созданный файл миграции
# Затем примените:
npx prisma migrate dev
```

---

## 🔍 Проверка состояния миграций

```powershell
# Проверить статус миграций
npx prisma migrate status

# Посмотреть историю миграций
# (файлы в prisma/migrations/)
```

---

## 🚨 Решение проблем

### Проблема: "Database schema is not in sync"

**Решение:**
```powershell
# Синхронизируйте схему
npx prisma db push

# Или создайте миграцию
npx prisma migrate dev --name sync_schema
```

### Проблема: Ошибка при применении миграции

**Решение:**
1. Проверьте логи ошибки
2. Если нужно, откатите миграцию вручную через SQL
3. Исправьте schema.prisma
4. Создайте новую миграцию

### Проблема: Изменения не применяются

**Решение:**
```powershell
# Убедитесь, что .env указывает на правильную БД
# Проверьте DATABASE_URL и DIRECT_URL

# Принудительно примените
npx prisma db push --force-reset  # ОСТОРОЖНО: удалит данные!

# Или через миграции
npx prisma migrate reset  # ОСТОРОЖНО: удалит данные!
```

---

## 📋 Чеклист после изменения schema.prisma

- [ ] Убедился, что `.env` настроен на Neon
- [ ] Применил изменения (`db push` или `migrate dev`)
- [ ] Сгенерировал Prisma Client (`npm run db:generate`)
- [ ] Проверил через Prisma Studio (`npm run db:studio`)
- [ ] Протестировал приложение (`npm run dev`)
- [ ] (Если используется) Закоммитил изменения в Git

---

## 💡 Краткая справка команд

| Команда | Когда использовать |
|---------|-------------------|
| `prisma db push` | Быстрое применение в разработке |
| `prisma migrate dev` | Создание миграций (рекомендуется) |
| `prisma migrate deploy` | Применение миграций в production |
| `prisma generate` | Генерация Prisma Client |
| `prisma studio` | Просмотр данных в браузере |
| `prisma migrate status` | Проверка статуса миграций |

---

**Помните:** После миграции на Neon данные уже там, нужно только применять изменения схемы! 🚀
